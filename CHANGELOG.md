# Changelog - Приложение для медитации

## v1.0 - 23 декабря 2025

### ✅ Основной функционал

**Архитектура:**
- Monorepo структура с `/client` и `/server`
- Backend: Node.js + Fastify + SQLite
- Frontend: React + Vite + Tailwind CSS
- Docker Compose для локальной разработки

**Страницы:**
- `/` - Каталог категорий медитаций
- `/catalog/:categoryId` - Список практик в категории
- `/practice/:practiceId` - Страница практики с аудиоплеером

**Функционал прослушивания:**
- Интеграция с Playerjs для воспроизведения аудио
- Счетчик прослушиваний (один запуск Play = одна запись после 30 сек)
- Визуальный таймер с индикацией текущей позиции
- Сохранение прогресса в SQLite БД
- Защита от множественных записей при промотке

**База данных:**
- Таблица `users` - пользователи (dev mode: user_id = 1)
- Таблица `user_progress` - история прослушиваний
- Подсчет количества прослушиваний для каждой практики

### 🐛 Исправленные проблемы

1. **Счетчик не сохранялся при перезагрузке**
   - Причина: ограничение UNIQUE в БД
   - Решение: удалено ограничение, разрешены множественные записи

2. **Таймер не срабатывал при первом открытии**
   - Причина: обработчики событий устанавливались с задержкой
   - Решение: система повторных попыток + MutationObserver

3. **Множественные записи при промотке**
   - Причина: таймер срабатывал при каждом пересечении 30 сек
   - Решение: один запуск Play = одна запись через 30 секунд

4. **Неправильная логика сброса состояния**
   - Причина: `setTimerActive(false)` вызывался до записи
   - Решение: упрощена логика с использованием setTimeout

### 📦 Зависимости

**Backend:**
- fastify, @fastify/cors, @fastify/secure-session
- @fastify/passport, @fastify/static
- sqlite3, dotenv

**Frontend:**
- react, react-dom, react-router-dom
- react-markdown, tailwindcss

### 🚀 Запуск

```bash
# Development
npm run dev

# Production
docker-compose up
```

---

## v1.1 - 24 декабря 2025

### ✨ Новые функции

**UI улучшения:**
- ✅ Добавлена ссылка "Назад в каталог" на странице категории
- ✅ Правильное склонение слова "раз" (1 раз, 2 раза, 5 раз)
- ✅ Изменен цвет блока "прослушивание засчитано" на фирменный светло-фиолетовый (#f4eafc)
- ✅ Визуальный таймер показывает обратный отсчет оставшегося времени практики
- ✅ Переработана верстка блока таймера - текст "Прослушивание засчитано" на отдельной строке
- ✅ Увеличен отступ между кнопками авторизации (space-y-4)
- ✅ Добавлен футер "Служба заботы" на всех страницах с ссылкой на Telegram
- ✅ Унифицированы вертикальные отступы (mb-6 и space-y-6 везде)

**Функциональность:**
- ✅ Предотвращение перехода устройства в режим сна во время прослушивания (Wake Lock API)
- ✅ Единая система авторизации через Google/Yandex OAuth
- ✅ Автоматическое переключение между DEV (без авторизации) и Production (с OAuth)

### 🔧 Технические улучшения

**Авторизация:**
- Создан модуль `auth.js` с настройкой OAuth стратегий
- Passport.js интеграция с Google и Yandex
- DEV режим: авторизация отключена, используется user_id = 1
- Production режим: требуется OAuth через Google или Yandex
- Защита маршрутов в зависимости от режима

**Wake Lock:**
- Автоматическая активация при нажатии Play
- Предотвращает переход устройства в сон во время медитации
- Автоматическое освобождение при уходе со страницы

### 📦 Новые зависимости

- `passport-yandex` - Yandex OAuth стратегия

### 📚 Документация

- ✅ Создан README.md с инструкциями по настройке
- ✅ Обновлен env.example с комментариями
- ✅ Добавлены инструкции по настройке OAuth

---

## Планируемые доработки (v1.2)

- [ ] Добавление новых категорий и практик
- [ ] Статистика прослушиваний (графики, аналитика)
- [ ] Избранные практики
- [ ] Напоминания о практиках

