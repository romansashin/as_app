# Исправление счетчика прослушиваний медитаций

## Проблема
1. Счетчик прослушиваний срабатывал не с первого раза
2. При перезагрузке или закрытии страницы прогресс не сохранялся
3. Таймер мог запускаться несколько раз

## Найденные ошибки

### 1. Отсутствие флага записи прогресса
В `PracticePage.jsx` не было флага `progressRecordedRef`, который предотвращает повторную запись прогресса при срабатывании эффекта несколько раз.

### 2. Неправильная обработка загрузки прогресса
При загрузке прогресса из API не было достаточного логирования и обработки случаев, когда `progressData[id]` undefined.

### 3. Недостаточное логирование
Отсутствовало логирование для отладки процесса записи и загрузки прогресса.

### 4. Функция handlePlayerPlay пересоздавалась на каждом рендере
Функция `handlePlayerPlay` не была обернута в `useCallback`, что приводило к её пересозданию на каждом рендере. Это вызывало сброс флага `hasTriggeredRef` в `AudioPlayer`, что могло препятствовать корректной работе таймера.

## Внесенные исправления

### В `/client/src/pages/PracticePage.jsx`:

1. **Добавлен флаг `progressRecordedRef`** - предотвращает повторную запись прогресса
2. **Улучшена загрузка прогресса** - добавлена проверка на существование `progressData` и корректная обработка undefined
3. **Добавлено логирование** - теперь в консоли браузера видны все этапы:
   - Загрузка прогресса
   - Запуск таймера
   - Запись прогресса
   - Обновление счетчика
4. **Исправлена логика сброса состояния** - флаги сбрасываются только после успешной записи прогресса
5. **Обернута функция `handlePlayerPlay` в `useCallback`** - предотвращает пересоздание функции на каждом рендере, что исправляет проблему со сбросом флага в AudioPlayer

### В `/server/server.js`:

1. **Улучшено логирование POST /api/progress** - добавлен вывод ID созданной записи
2. **Добавлена валидация** - логирование невалидных запросов

## Как проверить исправление

1. Откройте http://localhost:5173/practice/p1
2. Откройте консоль браузера (F12)
3. Нажмите Play на аудиоплеере
4. Должно появиться сообщение: "Starting 30-second timer"
5. Через 30 секунд должны появиться сообщения:
   - "30 seconds elapsed, recording progress for: p1"
   - "Progress recorded successfully"
   - "Fetched updated progress"
   - "Updated listen count for p1: X"
6. Счетчик "Прослушано X раз" должен увеличиться
7. Перезагрузите страницу - счетчик должен сохраниться

## Проверка в базе данных

```bash
cd /Users/rmnsshn/Dev/server
sqlite3 database.sqlite "SELECT practice_id, COUNT(*) as count FROM user_progress WHERE user_id = 1 GROUP BY practice_id;"
```

Должно показать количество прослушиваний для каждой практики.

## Дополнительное исправление (23:46)

### Проблема
После первого исправления таймер срабатывал, но прогресс не сохранялся в базу данных.

### Причина
В эффекте записи прогресса вызывался `setTimerActive(false)` СРАЗУ в начале блока, до отправки запроса на сервер. Это приводило к тому, что:
1. Состояние менялось синхронно
2. Асинхронная функция `recordProgress` выполнялась, но к моменту её завершения компонент мог уже размонтироваться или состояние могло измениться

### Решение
Перенесли вызов `setTimerActive(false)` в конец функции `recordProgress`, чтобы состояние сбрасывалось только ПОСЛЕ успешной записи прогресса на сервер.

### Добавлено подробное логирование
- ⏱️ Timer tick - каждую секунду
- ✅ 30 seconds elapsed - когда таймер достиг 0
- 📤 Sending progress - отправка на сервер
- ✅ Progress recorded - успешная запись
- 📥 Fetching updated progress - загрузка обновленных данных
- 📊 Fetched updated progress - полученные данные
- 🎯 Updated listen count - обновленный счетчик
- 🔄 State reset - сброс состояния
- ❌ Error recording progress - ошибка записи
- ⚠️ Updating count locally - локальное обновление при ошибке

## Критическое исправление #2 (00:00)

### Проблема 1: Ошибка 500 при сохранении прогресса
Сервер возвращал 500 Internal Server Error при попытке записать прогресс.

### Причина
В схеме базы данных было ограничение `UNIQUE(user_id, practice_id)`, которое позволяло только ОДНУ запись для каждой комбинации пользователя и практики. При попытке добавить вторую запись прослушивания той же практики возникала ошибка нарушения уникальности.

### Решение
1. Удалили ограничение UNIQUE из таблицы `user_progress`
2. Пересоздали таблицу с правильной схемой
3. Обновили `db.js` с комментарием о том, что UNIQUE не нужен
4. Улучшили логирование ошибок на сервере

### Проблема 2: Таймер не срабатывает при первом открытии практики
При первом открытии страницы с медитацией нажатие Play не запускало таймер.

### Причина
1. Обработчики событий устанавливались с задержкой в 1 секунду
2. Если пользователь нажимал Play раньше, событие не обрабатывалось
3. `hasTriggeredRef` сбрасывался при изменении `onPlay`, что могло вызывать проблемы
4. Аудио элементы могли появляться динамически после установки обработчиков

### Решение
1. Убрали сброс `hasTriggeredRef` при изменении `onPlay`
2. Реализовали систему повторных попыток установки обработчиков (до 10 попыток с интервалом 200мс)
3. Добавили `MutationObserver` для отслеживания динамически добавляемых аудио элементов
4. Улучшили обработку событий с множественными fallback механизмами:
   - Playerjs API события
   - Нативные события audio элементов
   - Обработчик кликов с проверкой состояния воспроизведения
5. Добавили подробное логирование для отладки:
   - 🎵 Audio play detected
   - 🔒 Play already triggered
   - 🎧 Audio element play event
   - 🎬 Playerjs API play event
   - 🔍 Found N audio elements
   - ➕ New audio element detected
   - 🖱️ Click detected
   - ▶️ Audio is playing
   - ✅ Handlers setup successful
   - 🧹 Cleaning up

## Улучшение UX: Визуальный таймер (00:15)

### Изменение
Вместо обратного отсчета "Осталось 30 секунд" теперь показывается реальное время воспроизведения в формате MM:SS.

### Что изменилось:

1. **Таймер считает вперед** (0:00 → 0:01 → 0:02 ... → 0:30 ...)
2. **Красивый UI с градиентом** и анимированной точкой
3. **Показывает прогресс**:
   - До 30 секунд: "До записи: X сек"
   - После 30 секунд: "✓ Прослушивание засчитано"
4. **Таймер продолжает работать** после записи прогресса (не сбрасывается)

### Визуальные улучшения:

```
┌─────────────────────────────────────────┐
│ ● 0:45        До записи: 15 сек        │  ← До 30 сек
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ ● 1:23    ✓ Прослушивание засчитано    │  ← После 30 сек
└─────────────────────────────────────────┘
```

### Технические детали:

- `timeRemaining` → `playbackTime` (прямой счет вместо обратного)
- Добавлена константа `REQUIRED_LISTENING_TIME = 30`
- Функция `formatTime()` для форматирования MM:SS
- Градиентный фон с анимированной точкой
- Флаги сбрасываются, но таймер продолжает работать

## Критическое исправление #3: Защита от промотки (00:30)

### Проблема
При промотке плеера вперед счетчик прослушиваний увеличивался многократно (до 27 раз за одно прослушивание).

### Причина
Таймер считал время по внутреннему setInterval, а не по реальному времени воспроизведения аудио. Когда пользователь промотал с 0:10 на 2:00, таймер продолжал тикать и каждый раз при пересечении отметки 30 секунд записывал новое прослушивание.

### Решение
Реализована система отслеживания **реального** прослушанного времени:

1. **Две метрики времени**:
   - `playbackTime` - текущая позиция в треке (визуальное)
   - `actualListeningTime` - реально прослушанные секунды

2. **Защита от промотки**:
   - Проверяем разницу между текущим и предыдущим временем
   - Если разница = 1 секунда → засчитываем
   - Если разница > 1 → промотка вперед, не засчитываем
   - Если разница < 0 → промотка назад, не засчитываем

3. **Логирование**:
   - `⏱️ Position: Xs, Actually listened: Ys`
   - `⏩ Forward seek detected (+Xs), not counting`
   - `⏪ Backward seek detected (-Xs), not counting`

### UI улучшения:
```
┌──────────────────────────────────────────┐
│ ● 1:45              До записи: 12 сек   │
│   Прослушано: 18с                       │
└──────────────────────────────────────────┘
```

- Верхняя строка: текущая позиция в треке
- Нижняя строка: сколько реально прослушано

### Результат
✅ Промотка вперед не увеличивает счетчик
✅ Нужно реально прослушать 30 секунд
✅ После 30 секунд прослушивание засчитывается один раз

## Финальное упрощение #4: Один запуск = одна запись (00:45)

### Проблема
Даже с защитой от промотки логика была слишком сложной. После первой записи при перемотке счетчик снова начинал увеличиваться.

### Решение
Максимально упростили логику:

**Один запуск Play = одна запись прослушивания**

### Как работает:

1. **Пользователь нажимает Play** → запускается таймер на 30 секунд
2. **Через 30 секунд** → автоматически записывается прослушивание
3. **Всё!** Больше ничего не записывается в этой сессии

### Что можно делать:

✅ Ставить на паузу и продолжать - это всё одно прослушивание  
✅ Перематывать вперед/назад - не влияет  
✅ Слушать сколько угодно - запишется только один раз  

### Что сбрасывает счетчик:

🔄 Только переход на другую страницу или перезагрузка  
🔄 Новое открытие той же практики = новая сессия = новая запись  

### Технические детали:

- Удалены все сложные проверки времени
- Используется простой `setTimeout(30000)` - 30 секунд
- Два флага:
  - `playEventFiredRef` - был ли нажат Play
  - `sessionRecordedRef` - записано ли прослушивание
- Визуальный таймер показывает текущую позицию в треке

### UI состояния:

```
До нажатия Play:
┌────────────────────────────────────────┐
│ Нажмите Play для начала прослушивания │
└────────────────────────────────────────┘

После Play (до 30 сек):
┌────────────────────────────────────────┐
│ ● 0:15   Прослушивание будет записано │
│          через 30 сек                  │
└────────────────────────────────────────┘

После 30 секунд:
┌────────────────────────────────────────┐
│ ● 1:23   ✓ Прослушивание засчитано    │
└────────────────────────────────────────┘
```

## Дата исправления
23 декабря 2025
