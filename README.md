# Приложение для медитации

**Версия:** 1.2.0  
**Дата:** 24 декабря 2025

Веб-приложение для прослушивания гипнопрактик и медитаций с отслеживанием прогресса.

📋 **[Release Notes v1.2](./RELEASE_v1.2.md)** - Что нового в этой версии

## Технологии

- **Backend**: Node.js, Fastify, SQLite
- **Frontend**: React, Vite, Tailwind CSS
- **Auth**: Passport.js (Google OAuth, Yandex OAuth)

## Быстрый старт (Development)

### 1. Установка зависимостей

```bash
npm install
cd server && npm install
cd ../client && npm install
cd ..
```

### 2. Запуск в DEV режиме (без авторизации)

```bash
npm run dev
```

Приложение будет доступно по адресу:
- Frontend: http://localhost:5173
- Backend: http://localhost:4000

**В DEV режиме авторизация отключена** - можно сразу пользоваться приложением.

**Новое:** Header с кнопкой "Выйти" теперь показывается и в DEV режиме для удобства разработки (с бейджем "DEV").

## Production (с авторизацией)

### 1. Настройка OAuth

#### Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/apis/credentials)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 Client ID
5. Добавьте Authorized redirect URIs:
   - `http://localhost:4000/auth/google/callback` (для dev)
   - `https://yourdomain.com/auth/google/callback` (для production)
6. Скопируйте Client ID и Client Secret

#### Yandex OAuth

1. Перейдите в [Yandex OAuth](https://oauth.yandex.ru/)
2. Создайте новое приложение
3. Укажите Callback URL:
   - `http://localhost:4000/auth/yandex/callback` (для dev)
   - `https://yourdomain.com/auth/yandex/callback` (для production)
4. Получите доступ к `login:email` и `login:info`
5. Скопируйте Client ID и Client Secret

### 2. Настройка переменных окружения

Создайте файл `/server/.env`:

```bash
cp server/env.example server/.env
```

Заполните переменные:

```env
NODE_ENV=production
PORT=4000
SESSION_SECRET=your-random-secret-key-here

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

YANDEX_CLIENT_ID=your-yandex-client-id
YANDEX_CLIENT_SECRET=your-yandex-client-secret

FRONTEND_URL=https://yourdomain.com
```

### 3. Сборка и запуск

```bash
# Сборка frontend
cd client && npm run build

# Запуск production сервера
cd ../server && npm start
```

## Docker

```bash
docker-compose up
```

## Структура проекта

```
/
├── client/              # React приложение
│   ├── src/
│   │   ├── pages/       # Страницы
│   │   ├── components/  # Компоненты
│   │   └── utils/       # API и утилиты
│   └── public/
├── server/              # Fastify сервер
│   ├── server.js        # Главный файл сервера
│   ├── auth.js          # OAuth конфигурация
│   ├── db.js            # База данных
│   └── data/            # JSON с контентом
└── docker-compose.yml
```

## Функционал

### Основной

- ✅ Каталог категорий медитаций
- ✅ Список практик в категории
- ✅ Аудиоплеер с визуальным таймером
- ✅ Счетчик прослушиваний (1 запуск = 1 запись после 30 сек)
- ✅ Сохранение прогресса в БД
- ✅ **Фоновое воспроизведение** - аудио продолжает играть даже с выключенным экраном
  - Wake Lock API - предотвращает засыпание устройства
  - Media Session API - управление с экрана блокировки
  - Автоматическое восстановление при возврате на страницу
  - Подробнее см. [AUDIO_PLAYBACK_FEATURES.md](./AUDIO_PLAYBACK_FEATURES.md)

### Авторизация

- ✅ DEV режим без авторизации (по умолчанию)
- ✅ Production с OAuth через Google/Yandex
- ✅ Автоматическое переключение между режимами
- ✅ Сохранение прогресса для каждого пользователя
- ✅ **Функция "Выйти"** - безопасный logout с сохранением прогресса
  - Кнопка в правом верхнем углу (только в production с OAuth)
  - Полная очистка сессии
  - Прогресс привязан к Google/Yandex аккаунту
  - Доступен с любого устройства после повторного входа
  - Подробнее см. [LOGOUT_FEATURE.md](./LOGOUT_FEATURE.md)

## База данных

SQLite с двумя таблицами:

- `users` - пользователи (provider, provider_id, email)
- `user_progress` - история прослушиваний

В DEV режиме используется фиктивный user_id = 1.

## Лицензия

MIT