# Инструкция по тестированию функции Logout

## Подготовка

### Требования
- ✅ OAuth должен быть настроен (Google или Yandex)
- ✅ Переменные окружения в `server/.env`
- ✅ Приложение запущено в режиме с авторизацией

### Проверка настройки OAuth

```bash
# Проверьте, что переменные заданы
cd server
cat .env | grep -E "GOOGLE_CLIENT_ID|YANDEX_CLIENT_ID"

# Если пусто - настройте OAuth (см. README.md)
```

### Запуск приложения

```bash
# Из корневой директории
npm run dev

# Должно показать в логах:
# "OAuth authentication enabled"
```

## Тест 1: Базовая функциональность

### Цель
Проверить, что кнопка "Выйти" работает корректно.

### Шаги

1. **Откройте приложение**
   ```
   http://localhost:5173
   ```

2. **Войдите через Google или Yandex**
   - Нажмите на кнопку входа
   - Авторизуйтесь
   - ✅ Должен перенаправить на главную страницу

3. **Проверьте Header**
   - В правом верхнем углу должен быть:
   ```
   👤 your.email@gmail.com    🚪 Выйти
   ```
   - ✅ Email должен быть виден
   - ✅ Кнопка "Выйти" должна быть кликабельна

4. **Навигация по страницам**
   - Откройте каталог
   - Откройте категорию
   - Откройте практику
   - ✅ Header должен оставаться на всех страницах

5. **Нажмите "Выйти"**
   - Кликните на кнопку "Выйти"
   - ✅ Должен перенаправить на `/login`
   - ✅ Header должен исчезнуть
   - ✅ Email не должен быть виден

6. **Проверьте доступ**
   - Попробуйте зайти на `/catalog`
   - ✅ Должен перенаправить на `/login`

### Ожидаемый результат
- Logout выполняется успешно
- Сессия очищается
- Доступ к защищённым страницам блокируется

---

## Тест 2: Сохранение прогресса

### Цель
Проверить, что прогресс сохраняется после logout.

### Шаги

1. **Войдите в приложение**
   - Авторизуйтесь через Google/Yandex

2. **Прослушайте практику**
   - Откройте любую практику
   - Нажмите Play
   - Подождите 30 секунд
   - ✅ Должно появиться "✓ Прослушивание засчитано"
   - ✅ Счётчик должен показать "Прослушано 1 раз"

3. **Запомните прогресс**
   - Запишите, сколько раз прослушана практика
   - Можно открыть несколько практик и прослушать их

4. **Выйдите из приложения**
   - Нажмите "Выйти"
   - ✅ Перенаправлено на `/login`

5. **Снова войдите**
   - Войдите через **тот же** Google/Yandex аккаунт
   - Откройте ту же практику

6. **Проверьте прогресс**
   - ✅ Счётчик должен показывать тот же прогресс
   - Например, если было "Прослушано 1 раз", должно остаться "Прослушано 1 раз"

### Ожидаемый результат
- Прогресс полностью сохранён
- Данные не потеряны после logout

---

## Тест 3: Смена аккаунта

### Цель
Проверить, что прогресс разных пользователей не смешивается.

### Требования
- 2 разных Google/Yandex аккаунта

### Шаги

1. **Войдите как Пользователь A**
   - Авторизуйтесь через аккаунт A
   - ✅ Header должен показать email A

2. **Создайте прогресс для A**
   - Прослушайте 3 практики (дождитесь засчитывания)
   - Запомните, какие именно практики

3. **Выйдите**
   - Нажмите "Выйти"

4. **Войдите как Пользователь B**
   - Авторизуйтесь через аккаунт B
   - ✅ Header должен показать email B

5. **Проверьте прогресс B**
   - Откройте те же практики, что прослушивал A
   - ✅ Счётчик должен быть пустой (или показывать свой прогресс, если B уже пользовался)
   - ✅ Не должен видеть прогресс пользователя A

6. **Создайте прогресс для B**
   - Прослушайте 2 другие практики
   - Дождитесь засчитывания

7. **Выйдите и снова войдите как A**
   - Нажмите "Выйти"
   - Войдите через аккаунт A
   - Откройте практики A

8. **Проверьте прогресс A**
   - ✅ Должен видеть свои 3 прослушивания
   - ✅ Не должен видеть 2 прослушивания пользователя B

### Ожидаемый результат
- Прогресс полностью изолирован
- Каждый пользователь видит только свои данные

---

## Тест 4: Несколько устройств

### Цель
Проверить, что прогресс синхронизируется между устройствами.

### Требования
- 2 устройства (например, телефон и компьютер)
- Один и тот же Google/Yandex аккаунт

### Шаги

1. **На устройстве 1 (телефон)**
   - Войдите в приложение
   - Прослушайте 5 практик
   - Запомните прогресс

2. **На устройстве 2 (компьютер)**
   - Откройте приложение
   - Войдите через **тот же** аккаунт
   - ✅ Должны видеть 5 прослушиваний с телефона

3. **На устройстве 2**
   - Прослушайте ещё 3 практики
   - Общий прогресс: 5 + 3 = 8

4. **На устройстве 1**
   - Обновите страницу (F5 или pull-to-refresh)
   - ✅ Должны видеть 8 прослушиваний

5. **Выйдите на устройстве 1**
   - Нажмите "Выйти"

6. **Проверьте устройство 2**
   - Обновите страницу на устройстве 2
   - ⚠️ Должны быть выкинуты на `/login` (сессия связана с устройством)
   - Это нормально - в текущей реализации сессии независимы

7. **Войдите снова на устройстве 1**
   - Авторизуйтесь
   - ✅ Должны видеть все 8 прослушиваний

### Ожидаемый результат
- Прогресс синхронизируется между устройствами
- Данные доступны с любого устройства

---

## Тест 5: DEV режим

### Цель
Проверить, что в DEV режиме без OAuth всё работает корректно.

### Подготовка

```bash
# Временно переименуйте .env
cd server
mv .env .env.backup

# Запустите приложение
cd ..
npm run dev

# В логах должно быть:
# "OAuth credentials not found - running in DEV mode"
```

### Шаги

1. **Откройте приложение**
   ```
   http://localhost:5173
   ```

2. **Проверьте Header**
   - ✅ Header не должен показываться
   - ✅ Нет кнопки "Выйти"
   - ✅ Нет отображения email

3. **Проверьте доступ**
   - Откройте `/catalog`
   - ✅ Должно открываться без авторизации
   - Откройте практику
   - ✅ Должно работать нормально

4. **Проверьте прогресс**
   - Прослушайте практику
   - ✅ Прогресс должен засчитываться
   - Обновите страницу
   - ✅ Прогресс должен сохраняться

5. **Восстановите .env**
   ```bash
   cd server
   mv .env.backup .env
   ```

### Ожидаемый результат
- DEV режим работает без OAuth
- Header не показывается
- Приложение полностью функционально

---

## Тест 6: Безопасность сессий

### Цель
Проверить, что сессия корректно очищается.

### Шаги

1. **Войдите в приложение**
   - Авторизуйтесь

2. **Откройте DevTools**
   - Нажмите F12
   - Перейдите на вкладку "Application" (Chrome) или "Storage" (Firefox)
   - Откройте "Cookies" → `http://localhost:5173`

3. **Проверьте cookies**
   - ✅ Должны видеть cookie с сессией (например, `session`)
   - Скопируйте значение cookie

4. **Нажмите "Выйти"**
   - Кликните на "Выйти"

5. **Снова проверьте cookies**
   - ✅ Cookie сессии должен быть удалён или изменён
   - Сравните с предыдущим значением
   - ✅ Значение должно отличаться (новая сессия)

6. **Попробуйте восстановить старый cookie**
   - Вручную установите старое значение cookie
   - Обновите страницу
   - Попробуйте зайти на `/catalog`
   - ✅ Должен перенаправить на `/login` (старая сессия недействительна)

### Ожидаемый результат
- Сессия полностью очищается
- Старые сессии не могут быть использованы

---

## Тест 7: Консольные логи

### Цель
Проверить корректность логирования.

### Шаги

1. **Откройте консоль браузера (F12)**

2. **Войдите в приложение**
   - Должны видеть:
   ```
   User not authenticated (DEV mode or no session) [до входа]
   ```

3. **После входа**
   - Header должен загрузить пользователя
   - Не должно быть ошибок

4. **Нажмите "Выйти"**
   - Логов может не быть (это нормально)
   - Не должно быть ошибок

5. **Проверьте логи сервера**
   - В терминале где запущен сервер:
   ```
   INFO: POST /auth/logout
   ```

### Ожидаемый результат
- Нет ошибок в консоли
- Логи сервера показывают logout

---

## Автоматизированный тест (опционально)

Если хотите автоматизировать тестирование:

```javascript
// test/logout.test.js
describe('Logout functionality', () => {
  it('should logout user and redirect to login', async () => {
    // Login
    await page.goto('http://localhost:5173');
    await page.click('#google-login-button');
    await page.waitForNavigation();
    
    // Check header
    const email = await page.$eval('header', el => el.textContent);
    expect(email).toContain('@');
    
    // Logout
    await page.click('button:has-text("Выйти")');
    await page.waitForNavigation();
    
    // Verify redirect
    expect(page.url()).toContain('/login');
    
    // Verify header is gone
    const header = await page.$('header');
    expect(header).toBeNull();
  });
  
  it('should preserve progress after logout', async () => {
    // Login, listen, check progress
    // Logout
    // Login again
    // Check progress is the same
  });
});
```

---

## Checklist

Перед завершением тестирования убедитесь:

### Функциональность
- [ ] Кнопка "Выйти" показывается в production с OAuth
- [ ] Кнопка "Выйти" НЕ показывается в DEV режиме
- [ ] Кнопка "Выйти" НЕ показывается на странице `/login`
- [ ] После logout перенаправляет на `/login`
- [ ] Header показывает правильный email
- [ ] Header присутствует на всех страницах (кроме `/login`)

### Прогресс
- [ ] Прогресс сохраняется после logout
- [ ] Прогресс доступен после повторного входа
- [ ] Прогресс не смешивается между пользователями
- [ ] Прогресс синхронизируется между устройствами

### Безопасность
- [ ] Сессия полностью очищается при logout
- [ ] Старые сессии не могут быть использованы повторно
- [ ] Доступ к защищённым страницам блокируется после logout

### UX
- [ ] Logout выполняется быстро (< 1 секунды)
- [ ] Нет мерцания или странного поведения
- [ ] Email корректно отображается (не обрезан)
- [ ] Кнопка "Выйти" хорошо видна и понятна

### Логи
- [ ] Нет ошибок в консоли браузера
- [ ] Логи сервера показывают успешный logout
- [ ] Нет warning'ов о проблемах с сессиями

---

## Известные ограничения

1. **Сессии независимы между устройствами**
   - Logout на одном устройстве НЕ выкидывает пользователя с других устройств
   - Это стандартное поведение (как в Gmail, Facebook и т.д.)
   - Если нужно - можно добавить "Выйти везде" в будущем

2. **DEV режим использует одного пользователя**
   - Все пользователи работают под `user_id = 1`
   - Это нормально для разработки

3. **Нет подтверждения logout**
   - При нажатии "Выйти" сразу происходит logout
   - Можно добавить модальное окно подтверждения

---

## Если что-то не работает

### Проблема: Кнопка "Выйти" не показывается

**Проверьте:**
1. Запущен ли сервер с OAuth? (`OAuth authentication enabled` в логах)
2. Авторизовались ли вы?
3. Нет ли ошибок в консоли браузера?

### Проблема: Прогресс не сохраняется

**Проверьте:**
1. Существует ли `database.sqlite` в папке `server/`?
2. Есть ли ошибки в логах сервера?
3. Корректно ли записывается прогресс до logout?

### Проблема: "Failed to logout: 401"

**Это не критично:**
- Сессия и так очистится локально
- Пользователь будет перенаправлен на `/login`
- Ошибка означает, что сессии уже не было на сервере

### Проблема: Не перенаправляет на /login

**Проверьте:**
1. Работает ли React Router?
2. Есть ли ошибки в консоли?
3. Попробуйте обновить страницу после logout

---

## Заключение

После прохождения всех тестов функция logout считается полностью работоспособной. 

✅ Готово к использованию в production!

