# 🚀 Deployment Checklist v1.2

## Перед деплоем в Production

### 📋 Обязательные проверки

#### 1. Environment Variables
- [ ] `NODE_ENV=production`
- [ ] `PORT=4000` (или ваш порт)
- [ ] `SESSION_SECRET` установлен (32+ символов, криптостойкий)
- [ ] `GOOGLE_CLIENT_ID` настроен
- [ ] `GOOGLE_CLIENT_SECRET` настроен
- [ ] `YANDEX_CLIENT_ID` настроен
- [ ] `YANDEX_CLIENT_SECRET` настроен
- [ ] `FRONTEND_URL` указывает на production домен (HTTPS)
- [ ] Файл `.env` в `.gitignore`

**Генерация SESSION_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### 2. OAuth настройка

**Google Cloud Console:**
- [ ] Добавлен production redirect URI: `https://yourdomain.com/auth/google/callback`
- [ ] Добавлен production домен в Authorized domains
- [ ] OAuth consent screen настроен

**Yandex OAuth:**
- [ ] Добавлен production callback URL: `https://yourdomain.com/auth/yandex/callback`
- [ ] Права доступа: `login:email`, `login:info`

#### 3. Сборка Frontend
```bash
cd client
npm run build
```
- [ ] Build выполнен без ошибок
- [ ] Файлы в `client/dist/` созданы
- [ ] `client/dist/index.html` существует

#### 4. База данных
- [ ] `server/database.sqlite` существует (будет создан автоматически)
- [ ] Права доступа к файлу корректны
- [ ] Backup стратегия определена
- [ ] Таблицы `users` и `user_progress` будут созданы при первом запуске

#### 5. Безопасность
- [ ] HTTPS настроен (для secure cookies)
- [ ] Firewall настроен (только нужные порты открыты)
- [ ] CORS origin указывает только на ваш домен
- [ ] Rate limiting рассмотрен (опционально)
- [ ] Логирование настроено

#### 6. Код
- [ ] Нет `console.log` с конфиденциальной информацией
- [ ] Нет hardcoded секретов
- [ ] Линтер проверен (`npm run lint` если есть)
- [ ] Dependencies обновлены (`npm audit`)

---

## 🧪 Тестирование

### DEV режим (перед деплоем)
```bash
npm run dev
```

- [ ] Приложение запускается без ошибок
- [ ] `/catalog` открывается
- [ ] Аудиоплеер работает
- [ ] Прогресс записывается
- [ ] Header показывается (DEV режим)
- [ ] Кнопка "Выйти" работает

### Production режим (локально)
```bash
cd client && npm run build
cd ../server && NODE_ENV=production npm start
```

- [ ] Сервер запускается на порту 4000
- [ ] Static файлы отдаются корректно
- [ ] OAuth логин работает (если настроен)
- [ ] Header показывает email
- [ ] Logout перенаправляет на `/login`
- [ ] Прогресс сохраняется после logout

---

## 📦 Деплой

### Вариант 1: VPS/Dedicated Server

**Требования:**
- Node.js 16+
- npm 8+
- systemd или PM2 для process management

**Шаги:**

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd <project-directory>
```

2. **Установите зависимости:**
```bash
npm install
cd server && npm install
cd ../client && npm install
cd ..
```

3. **Настройте .env:**
```bash
cp server/env.example server/.env
nano server/.env  # или vim, или другой редактор
```

4. **Соберите frontend:**
```bash
cd client
npm run build
cd ..
```

5. **Запустите через PM2:**
```bash
npm install -g pm2
cd server
pm2 start server.js --name "meditation-app"
pm2 save
pm2 startup
```

6. **Настройте Nginx (reverse proxy):**
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Вариант 2: Docker

**Использовать существующий `docker-compose.yml`:**

1. **Настройте .env:**
```bash
cp server/env.example server/.env
nano server/.env
```

2. **Соберите и запустите:**
```bash
docker-compose up -d --build
```

3. **Проверьте логи:**
```bash
docker-compose logs -f
```

### Вариант 3: Cloud Platforms

**Heroku:**
- Добавьте `Procfile`
- Настройте environment variables
- Deploy через Git

**Railway/Render:**
- Подключите GitHub репозиторий
- Настройте environment variables
- Auto-deploy при push

---

## ✅ После деплоя

### Проверка работоспособности

**1. Базовые проверки:**
- [ ] Сайт открывается по HTTPS
- [ ] Нет ошибок в консоли браузера (F12)
- [ ] `/health` возвращает `{"status":"ok"}`

**2. Авторизация:**
- [ ] Кнопки "Войти через Google/Yandex" работают
- [ ] OAuth redirect работает корректно
- [ ] После входа показывается Header с email
- [ ] Кнопка "Выйти" работает и перенаправляет на `/login`

**3. Функциональность:**
- [ ] Каталог категорий загружается
- [ ] Практики в категории загружаются
- [ ] Аудиоплеер проигрывает звук
- [ ] Прогресс записывается (после 30 сек)
- [ ] Прогресс сохраняется после logout

**4. Фоновое воспроизведение (Mobile):**
- [ ] Аудио играет с выключенным экраном (iOS/Android)
- [ ] Элементы управления на экране блокировки
- [ ] Wake Lock активируется (консоль браузера)

**5. Производительность:**
- [ ] Время загрузки < 3 сек
- [ ] Аудио загружается без задержек
- [ ] API отвечает быстро (< 500ms)

---

## 🔍 Мониторинг

### Логи

**PM2:**
```bash
pm2 logs meditation-app
pm2 monit
```

**Docker:**
```bash
docker-compose logs -f app
```

**Systemd:**
```bash
journalctl -u meditation-app -f
```

### Метрики для отслеживания

- [ ] Uptime
- [ ] Response time
- [ ] Error rate
- [ ] Memory usage
- [ ] CPU usage
- [ ] Database size
- [ ] Active users

### Рекомендуемые инструменты

- **Логирование:** Sentry, LogRocket, Papertrail
- **Uptime monitoring:** UptimeRobot, Pingdom
- **Analytics:** Google Analytics, Plausible
- **Performance:** New Relic, DataDog

---

## 🔐 Безопасность

### SSL/TLS
- [ ] HTTPS работает
- [ ] Redirect с HTTP на HTTPS настроен
- [ ] SSL certificate валидный (Let's Encrypt или другой)

### Headers
Рекомендуется добавить в Nginx/Apache:
```
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Strict-Transport-Security: max-age=31536000; includeSubDomains
```

### Backup
- [ ] Автоматический backup базы данных настроен
- [ ] Backup тестируется регулярно
- [ ] Recovery план документирован

---

## 📊 Post-Launch

### Неделя 1
- [ ] Мониторинг ошибок
- [ ] Проверка логов ежедневно
- [ ] Сбор feedback от пользователей
- [ ] Проверка производительности

### Месяц 1
- [ ] Анализ использования
- [ ] Оптимизация производительности
- [ ] Обновление зависимостей
- [ ] Планирование новых функций

---

## 🆘 Rollback Plan

Если что-то пошло не так:

**1. Откатите код:**
```bash
git checkout <previous-tag>
pm2 restart meditation-app
```

**2. Восстановите базу данных:**
```bash
cp database.sqlite.backup database.sqlite
pm2 restart meditation-app
```

**3. Проверьте логи:**
```bash
pm2 logs meditation-app --lines 100
```

**4. Уведомите пользователей:**
- Обновите статус на главной странице
- Отправьте уведомление в Telegram

---

## 📞 Контакты

**В случае проблем:**
- Telegram: [@as_administrator](https://t.me/as_administrator)
- Email: (если есть)
- GitHub Issues: (если есть)

---

## ✅ Финальный чеклист

Перед тем как отметить деплой завершённым:

- [ ] Все обязательные проверки пройдены
- [ ] Тестирование в production выполнено
- [ ] Мониторинг настроен
- [ ] Backup стратегия активна
- [ ] Команда уведомлена о релизе
- [ ] Документация обновлена
- [ ] Release notes опубликованы

---

**Версия:** 1.2.0  
**Дата:** 24 декабря 2025

🎉 **Успешного деплоя!**

